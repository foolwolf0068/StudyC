#include <stdio.h>
#include <stdlib.h>

void Print_Factorial ( const int N );

int main()
{
    int N;

    scanf("%d", &N);
    Print_Factorial(N);
    return 0;
}

/* 你的代码将被嵌在这里 */
void Print_Factorial ( const int N )
{
    if(N<0||N>1000)
    {
        printf("Invalid input\n");
    }
    else if(N<=1)
    {
        printf("%d\n",1);
    }
    else
    {
        
    }
}
