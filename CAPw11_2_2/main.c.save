#include <stdio.h>
#include <stdlib.h>
struct point{
    int x;
    int y;
};
void getStruct(struct point);
void output(struct point);
int main()
{
    printf("Hello world!\n");
    return 0;
}
void getStruct(struct point P)
{
    scanf("%d", &P.x);
    scanf("%d", &P)
}