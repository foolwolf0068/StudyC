#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,j,N;
    scanf("%d", &N);
    for(i=1;i<=N;i++)
    {
        for(j=1;j<=i;j++)
        {
            printf("%d*%d=%-4d",j,i,j*i);
        }
        printf("\n");
    }
    int b;
    double a,d;
    char c;
    scanf("%f %d %c %f", &a,)
    return 0;
}
