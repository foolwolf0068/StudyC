#include <stdio.h>
#include <stdlib.h>

int main()
{
    int Sum, X;
    char Op;
    scanf("%d %c", &Sum, &Op);
    while (Op != '=')
    {
        scanf("%d", &X);
        switch(Op)
        {
        case '+':
            Sum += X;
            break;
        case '-':
            Sum -= X;
            break;
        case '*':
            Sum *= X;
            break;
        case '/':
            if (X == 0)
            {
                printf("·ÖÄ¸ÎªÁã£¡\n");
                return 0;
            }
            Sum /=
        }
    }
    printf("Hello world!\n");
    return 0;
}
